name: maniak_game_deals
description: A new Flutter project.
publish_to: 'none'
version: 0.3.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^3.0.0
  carousel_slider: ^4.0.0-nullsafety.0
  cupertino_icons: ^1.0.3
  dio: ^4.0.0
  flutter_staggered_grid_view: ^0.4.0
  rxdart: ^0.26.0
  url_launcher: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.5.3

flutter:
  uses-material-design: true
  assets:
    - assets/img/stores/icons/
